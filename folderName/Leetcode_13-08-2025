class Solution {
    public boolean isPowerOfThree(int n) {
        if(n<=0) return false;
        double a =(Math.log(n)/Math.log(3));
        if(Math.abs(a-Math.round(a)) < 1e-10&&a<=19) return true;
        return false;
        
    }
}
